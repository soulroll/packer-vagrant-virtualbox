# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|

    config.vm.box = "file://builds/vagrant.box"
    config.vm.network "private_network", ip: "192.168.33.10"
    config.vm.hostname = "xenial64"

end
